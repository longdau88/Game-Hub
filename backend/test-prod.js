const https = require('https');

async function main() {
  const loginData = JSON.stringify({ email: "admin@example.com", password: "password", username: "admin" }); // Assuming admin username/password? Wait I don't know the admin password.
}
main();
