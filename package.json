{
  "name": "game-hub-monolith",
  "version": "1.0.0",
  "description": "Game Hub Monorepo Root",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npx prisma generate",
    "start": "cd backend && npm start"
  }
}
